apiVersion: v1
kind: Service
metadata:
  name: changedetection
spec:
  type: LoadBalancer
  selector:
    app: changedetection
  ports:
    - name: http
      port: 5000
      targetPort: http
      protocol: TCP